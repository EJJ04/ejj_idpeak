Config = {}

-- Keybind for toggling ID peek
Config.Keybind = 'PAGEUP'

-- Maximum distance to show IDs
Config.MaxDistance = 15.0

-- 3D Text Settings
Config.Text = {
    scale = 0.6,        -- Size of text
    font = 4,           -- GTA font style
    color = {255, 255, 255, 215}, -- RGBA (Red, Green, Blue, Alpha)
    offsetZ = 1.2       -- How high above the player the text appears
}

-- Performance Settings
Config.UpdateInterval = 0 -- Lower is smoother but higher is more optimized (default 0)
